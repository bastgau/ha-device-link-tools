# Runtime: Home Assistant + build tools for the custom component.
# Default_config requirements moved to .devcontainer/requirements-default-config.txt
# to exclude them from Dependabot's scan (not relevant to the integration itself).
homeassistant==2026.9.0b3
prek==0.5.2
